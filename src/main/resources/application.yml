spring:
  application:    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}          search-paths:
          - User-Microservice*
          - Eureka-Server*
          - Front-End-Service*